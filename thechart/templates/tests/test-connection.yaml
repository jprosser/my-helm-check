apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thechart.fullname" . }}-test-connection"
  labels:
    {{- include "thechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
